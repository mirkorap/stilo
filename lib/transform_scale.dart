library stilo;

import 'package:flutter/material.dart';

abstract class StiloScale {
  static Transform s0({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 0.0,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s50({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 0.5,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s75({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 0.75,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s90({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 0.9,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s95({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 0.95,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s100({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 1.0,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s105({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 1.05,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s110({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 1.1,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s125({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 1.25,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }

  static Transform s150({
    Key key,
    Offset origin,
    AlignmentGeometry alignment = Alignment.center,
    Widget widget,
  }) {
    return Transform.scale(
      scale: 1.5,
      key: key,
      origin: origin,
      alignment: alignment,
      child: widget,
    );
  }
}
